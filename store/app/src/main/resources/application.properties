# Server
server.port=0
server.reactive.session.cookie.name=REACTIVE_${spring.application.name}
server.servlet.session.cookie.name=SERVLET_${spring.application.name}

# Application
spring.application.name=store
spring.profiles.default=local
spring.profiles.active=local

# Vendix
vendix.security.jwt.private-key=JSGJEkw471n4i0H4aX5sQamrTnXL/iKwTfTq3YLAWYA=
vendix.orchestrator.client.base-url=${VENDIX_ORCHESTRATOR_BASE_URL:http://localhost:8080}

# Monitoring
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.prometheus.access=read_only

vendix.testing.simulate-random-article-order=false

# AMQP Messaging
amqp.central-exchange=central
amqp.central-application=${spring.application.name}
