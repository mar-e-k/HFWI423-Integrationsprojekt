# --- Server ---
server.reactive.session.cookie.name=REACTIVE_${spring.application.name}
server.servlet.session.cookie.name=SERVLET_${spring.application.name}

# --- App ---
spring.application.name=filialensystem
spring.profiles.active=test

# --- App Config ----

# --- App Startup Config ----
spring.filialensystem.startup.cleanup-locks-hosts=true
spring.filialensystem.startup.setup-store-client=true

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- Datasource ---
spring.datasource.driver-class-name=org.postgresql.Driver

# --- RabbitMQ ---
spring.rabbitmq.exchange=${spring.application.name}.${spring.profiles.active}
spring.rabbitmq.ssl.enabled=true
spring.rabbitmq.host=cow.rmq2.cloudamqp.com
spring.rabbitmq.port=5671
spring.rabbitmq.username=tgamtfjc
spring.rabbitmq.password=LGl-i3KF-JuaNxoPrUGCNsffR9AYfo-i
spring.rabbitmq.virtual-host=tgamtfjc

# --- Actuator ---
management.endpoints.web.exposure.include=health, info