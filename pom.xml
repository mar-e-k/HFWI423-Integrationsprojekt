<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.0</version>
        <relativePath/>
    </parent>

    <groupId>de.fhdw.vendix</groupId>
    <artifactId>vendix-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>vendix</name>
    <description>vendix</description>

    <modules>
        <module>pos</module>
        <module>store</module>
        <module>commons</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <vaadin.version>25.0.5</vaadin.version>
        <swagger.version>2.2.42</swagger.version>
        <pdfbox.version>3.0.6</pdfbox.version>
        <plaguv-amqp.version>1.1.0</plaguv-amqp.version>
        <nimbus.version>10.7</nimbus.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Source: https://mvnrepository.com/artifact/com.vaadin/vaadin-bom -->
            <dependency>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-bom</artifactId>
                <version>${vaadin.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/io.swagger.core.v3/swagger-annotations -->
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations</artifactId>
                <version>${swagger.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.apache.pdfbox/pdfbox -->
            <dependency>
                <groupId>org.apache.pdfbox</groupId>
                <artifactId>pdfbox</artifactId>
                <version>${pdfbox.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/io.github.plaguv/plaguv-amqp-spring-boot-starter -->
            <dependency>
                <groupId>io.github.plaguv</groupId>
                <artifactId>plaguv-amqp-spring-boot-starter</artifactId>
                <version>${plaguv-amqp.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/com.nimbusds/nimbus-jose-jwt -->
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>nimbus-jose-jwt</artifactId>
                <version>${nimbus.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>