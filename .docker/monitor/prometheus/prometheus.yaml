global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'vendix-apps'
    metrics_path: /actuator/prometheus
    consul_sd_configs:
      - server: 'consul:8500'
        services: []