<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <springProperty scope="context" name="__service_name__" source="spring.application.name" defaultValue="unknown"/>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">

        <labels>
            app = vendix
            * = %%mdc{service_name, service_id, host, instance}
        </labels>

        <structuredMetadata>
            level = %level
            thread = %thread
            logger = %logger
        </structuredMetadata>

        <message>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </message>

        <http>
            <url>${LOKI_URL:-http://localhost:3100/loki/api/v1/push}</url>
        </http>

        <batch>
            <maxItems>100</maxItems>
            <timeoutMs>500</timeoutMs>
        </batch>

    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOKI"/>
    </root>
</configuration>