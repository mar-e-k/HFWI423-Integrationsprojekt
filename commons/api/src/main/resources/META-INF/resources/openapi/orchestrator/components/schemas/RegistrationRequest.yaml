type: object
required: [instanceId, appType, port]
properties:
  instanceId:
    type: string
    example: "uuid-123"
  appType:
    type: string
    enum: [store, pos]
  port:
    type: integer
    example: 54321
  context:
    type: string
    description: "Optional: Store ID or POS ID if already known"