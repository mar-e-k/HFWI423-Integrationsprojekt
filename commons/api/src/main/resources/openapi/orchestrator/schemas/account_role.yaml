AccountRoleDTO:
  type: object
  description: Represents a role assigned to an account.
  properties:
    id:
      type: integer
      format: int64
    role:
      $ref: "#/components/schemas/Role"
  example:
    id: 1
    role: "ADMIN"